currentStep?.id === 3 ? (
        <div className="flex flex-col gap-4">
          <h2 className="font-black text-gray-800">
            نمونه کار های خود را بارگذاری کنید
          </h2>

          <div className="w-full grid grid-cols-9 gap-4">
            <div className="w-full col-span-6 flex flex-col gap-2">
              <Input
                type="text"
                label="عنوان نمونه کار"
                placeholder="عنوان نمونه کار خود را وارد کنید"
                variant="bordered"
                labelPlacement="outside"
                classNames={{
                  inputWrapper:
                    "border border-gray-200 focus-within:!border-gray-400 shadow-none",
                }}
                isInvalid={errors.work_title ? true : false}
                errorMessage="عنوان نمونه کار اجباری میباشد"
                {...register("work_title", { required: true })}
              />

              <Textarea
                variant="bordered"
                label="توضیحات نمونه کار"
                placeholder="توضیحات نمونه کار خود را وارد کنید"
                labelPlacement="outside"
                classNames={{
                  inputWrapper:
                    "border border-gray-200 focus-within:!border-gray-400 shadow-none",
                }}
                className="md:col-span-2"
                isInvalid={errors.work_description ? true : false}
                errorMessage="توضیحات نمونه کار اجباری است"
                {...register("work_description", { required: true })}
              />

              <Button
                onClick={newWorkExamplesHandler}
                variant="shadow"
                className="bg-success text-white"
              >
                ثبت نمونه کار
              </Button>
            </div>

            {/* uploader */}
            <div className="w-full col-span-3">
              <div className="w-full flex h-60 relative group border border-gray-200 transition-all duration-300 border-dashed rounded-2xl">
                {workImage?.name ? (
                  <img
                    src={workImageUrl}
                    className="w-full rounded-xl"
                    alt=""
                  />
                ) : (
                  <div className="w-full h-full flex flex-col gap-3 items-center justify-center absolute top-0 right-0">
                    <div className="w-full h-full flex flex-col gap-3 items-center justify-center">
                      <FaCloudUploadAlt className="size-16 text-gray-400 group-hover:-translate-y-2 transition-all duration-300" />

                      <span className="text-gray-400 text-sm">
                        برای بارگذاری تصویر کلیک کنید
                      </span>
                      <p className="text-xs text-gray-500 text-center -mt-1">
                        (تصویر می بایست بصورت عمودی باشد)
                      </p>
                    </div>
                  </div>
                )}

                <Input
                  isRequired
                  classNames={{
                    helperWrapper:
                      "absolute bottom-0 left-2/4 -translate-x-2/4",
                    inputWrapper:
                      "absolute h-full opacity-0 top-0 w-full left-0 p-0 z-50",
                    input: "w-full h-full cursor-pointer bottom-0 top-0 left-0",
                  }}
                  type="file"
                  placeholder="تصویر خود را انتخاب کنید"
                  {...register("work_image", { required: true })}
                />
              </div>

              {errors?.work_image && (
                <span className="mt-2 block text-danger text-xs">
                  تصویر کارت ملی الزامی میباشد
                </span>
              )}
            </div>
          </div>

          <h3 className="font-black text-gray-800">
            نمونه کار های بارگذاری شده
          </h3>

          {workExamples.length > 0 ? (
            <div className="w-full grid grid-cols-4">
              {workExamples.map((example) => (
                <div className="w-full relative">
                  <img
                    src={example?.imageUrl}
                    className="w-full rounded-2xl"
                    alt=""
                  />

                  <div className="absolute rounded-b-2xl bg-gradient-to-t from-black to-transparent flex items-center w-full bottom-0 h-16 px-4 text-white">
                    <span>{example?.title}</span>
                  </div>
                </div>
              ))}
            </div>
          ) : (
            <p className="text-gray-700">نمونه کاری بارگذاری نکرده اید !</p>
          )}
        </div>
      ) : //  step 3 - add address
      currentStep?.id === 4 ? (
        <div className="flex flex-col gap-4">
          <h2 className="font-black text-gray-800">
            آدرس مرکز خود را با دقت وارد کنید
          </h2>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            <Controller
              name="province"
              control={control}
              render={({ field: { onChange, onBlur, value } }) => (
                <Select
                  variant="bordered"
                  required
                  placeholder="استان خود را انتخاب کنید"
                  isLoading={provincesLoading}
                  label={
                    <>
                      <span>استان</span>
                    </>
                  }
                  labelPlacement="outside"
                  size="md"
                  value={watch("province")}
                  selectedKeys={value ? [value] : []}
                  onBlur={onBlur}
                  onChange={onChange}
                  isInvalid={errors.province ? true : false}
                  errorMessage="انتخاب استان اجباری میباشد"
                  classNames={{
                    trigger:
                      "border border-gray-200 focus-within:!border-gray-400 shadow-none",
                  }}
                  {...register("province", { required: true })}
                >
                  {provinces.map((province) => (
                    <SelectItem key={province.id}>{province.name}</SelectItem>
                  ))}
                </Select>
              )}
            />

            <Controller
              name="province"
              control={control}
              render={({ field: { onChange, onBlur, value } }) => (
                <Select
                  variant="bordered"
                  required
                  placeholder="شهر خود را انتخاب کنید"
                  isLoading={provincesLoading}
                  label={
                    <>
                      <span>شهر</span>
                    </>
                  }
                  labelPlacement="outside"
                  size="md"
                  value={watch("province")}
                  selectedKeys={value ? [value] : []}
                  onBlur={onBlur}
                  onChange={onChange}
                  isInvalid={errors.province ? true : false}
                  errorMessage="انتخاب شهر اجباری میباشد"
                  classNames={{
                    trigger:
                      "border border-gray-200 focus-within:!border-gray-400 shadow-none",
                  }}
                  {...register("province", { required: true })}
                >
                  {provinces.map((province) => (
                    <SelectItem key={province.id}>{province.name}</SelectItem>
                  ))}
                </Select>
              )}
            />

            <div className="w-full md:col-span-2">
              <Input
                type="text"
                label="آدرس"
                placeholder="آدرس مرکز را وارد کنید"
                variant="bordered"
                labelPlacement="outside"
                classNames={{
                  inputWrapper:
                    "border border-gray-200 focus-within:!border-gray-400 shadow-none",
                }}
                isInvalid={errors.phone_number ? true : false}
                errorMessage="آدرس مرکز اجباری میباشد"
                {...register("phone_number", { required: true })}
              />
            </div>

            <div className="flex flex-col md:col-span-2 gap-2">
              <span className="text-sm text-foreground">
                آدرس مرکز خود را از روی نقشه انتخاب کنید
              </span>

              <div className="w-full h-96 relative rounded-2xl overflow-hidden">
                <MapContainer
                  ref={map}
                  center={location}
                  zoom={13}
                  scrollWheelZoom={true}
                >
                  <TileLayer
                    attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
                    url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"
                  />

                  <MapEvents />

                  <Marker icon={PointIcon} position={location}></Marker>
                </MapContainer>
              </div>
            </div>
          </div>
        </div>
      ) : (
        // step 3 - upload license
        currentStep?.id === 5 && (
          <div className="flex flex-col gap-6">
            <h2 className="font-black text-gray-800">
              بارگذاری مدارک و احراز هویت
            </h2>

            {/* sallon licens */}
            <div className="flex flex-col gap-3">
              {/* select picture */}
              <div className="w-full flex flex-col">
                <div className="mb-2 flex flex-col gap-2">
                  <span className="text-foreground text-sm">
                    تصویر جواز کسب مرکز را بارگذاری کنید
                  </span>
                </div>

                <div className="w-full flex h-44 relative group border border-gray-200 transition-all duration-300 border-dashed rounded-2xl">
                  <div className="w-full h-full flex flex-col gap-3 items-center justify-center absolute top-0 right-0 ">
                    <div className="w-full h-full flex flex-col gap-3 items-center justify-center">
                      <FaCloudUploadAlt className="size-16 text-gray-400 group-hover:-translate-y-2 transition-all duration-300" />

                      <span className="text-gray-400 text-sm">
                        پرونده ها را اینجا رها کنید یا برای بارگذاری کلیک کنید.
                      </span>
                      <p className="text-xs text-gray-500 text-center -mt-1">
                        (مدرک بارگذاری شده می بایست بصورت افقی، واضح و خوانا
                        باشد)
                      </p>
                    </div>
                  </div>

                  <Input
                    isRequired
                    classNames={{
                      helperWrapper:
                        "absolute bottom-0 left-2/4 -translate-x-2/4",
                      inputWrapper:
                        "absolute h-full opacity-0 top-0 w-full left-0 p-0 z-50",
                      input:
                        "w-full h-full cursor-pointer bottom-0 top-0 left-0",
                    }}
                    type="file"
                    placeholder="تصویر خود را انتخاب کنید"
                    {...register("sallon_license", { required: true })}
                  />
                </div>

                {errors?.sallon_license && (
                  <span className="mt-2 block text-danger text-xs">
                    مدرک جواز کسب الزامی میباشد
                  </span>
                )}
              </div>

              {/* images */}
              {sallonLicesnse?.name && (
                <div className="w-full flex justify-between items-center border border-borderColor p-3 rounded-xl">
                  <div className="flex items-center gap-2">
                    <img
                      src={sallonLicesnseUrl}
                      className="w-16 rounded-lg"
                      alt=""
                    />

                    <div className="flex flex-col gap-1">
                      <span className="text-sm text-gray-700">
                        {sallonLicesnse.name}
                      </span>
                      <span className="text-xs text-gray-500">
                        {toFarsiNumber(sallonLicesnseSize)}
                      </span>
                    </div>
                  </div>

                  <button
                    className="flex"
                    onClick={() => setValue("sallon_license", "")}
                  >
                    <i className="fi fi-rr-cross-small text-red-600 text-2xl h-6"></i>
                  </button>
                </div>
              )}
            </div>

            {/* national card */}
            <div className="flex flex-col gap-3">
              {/* select picture */}
              <div className="w-full flex flex-col">
                <div className="mb-2 flex flex-col gap-2">
                  <span className="text-foreground text-sm">
                    تصویر کارت ملی خود را بارگذاری کنید
                  </span>
                </div>

                <div className="w-full flex h-44 relative group border border-gray-200 transition-all duration-300 border-dashed rounded-2xl">
                  <div className="w-full h-full flex flex-col gap-3 items-center justify-center absolute top-0 right-0 ">
                    <div className="w-full h-full flex flex-col gap-3 items-center justify-center">
                      <FaCloudUploadAlt className="size-16 text-gray-400 group-hover:-translate-y-2 transition-all duration-300" />

                      <span className="text-gray-400 text-sm">
                        پرونده ها را اینجا رها کنید یا برای بارگذاری کلیک کنید.
                      </span>
                      <p className="text-xs text-gray-500 text-center -mt-1">
                        (مدرک بارگذاری شده می بایست بصورت افقی، واضح و خوانا
                        باشد)
                      </p>
                    </div>
                  </div>

                  <Input
                    isRequired
                    classNames={{
                      helperWrapper:
                        "absolute bottom-0 left-2/4 -translate-x-2/4",
                      inputWrapper:
                        "absolute h-full opacity-0 top-0 w-full left-0 p-0 z-50",
                      input:
                        "w-full h-full cursor-pointer bottom-0 top-0 left-0",
                    }}
                    type="file"
                    placeholder="تصویر خود را انتخاب کنید"
                    {...register("national_card", { required: true })}
                  />
                </div>

                {errors?.national_card && (
                  <span className="mt-2 block text-danger text-xs">
                    تصویر کارت ملی الزامی میباشد
                  </span>
                )}
              </div>

              {/* images */}
              {nationalCard?.name && (
                <div className="w-full flex justify-between items-center border border-borderColor p-3 rounded-xl">
                  <div className="flex items-center gap-2">
                    <img
                      src={nationalCardUrl}
                      className="w-16 rounded-lg"
                      alt=""
                    />

                    <div className="flex flex-col gap-1">
                      <span className="text-sm text-gray-700">
                        {nationalCard.name}
                      </span>
                      <span className="text-xs text-gray-500">
                        {toFarsiNumber(nationalCardSize)}
                      </span>
                    </div>
                  </div>

                  <button
                    className="flex"
                    onClick={() => setValue("national_card", "")}
                  >
                    <i className="fi fi-rr-cross-small text-red-600 text-2xl h-6"></i>
                  </button>
                </div>
              )}
            </div>
          </div>
        )
      )